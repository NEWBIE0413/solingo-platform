DATABASE_URL=postgres://solingo:solingo@localhost:5433/solingo
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Clerk (https://dashboard.clerk.com → API keys). Required: the app does not boot without them.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_ADMIN_IDS=
# Stripe — optional; only the shop's "unlimited hearts" upgrade uses it
STRIPE_API_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
